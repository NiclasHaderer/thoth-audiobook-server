jdk:
  # Only bootstraps the image: JitPack's jdk: values lag behind current JDKs, so sdkman
  # below provides the JDK 25 the build actually compiles with.
  - openjdk17
before_install:
  # https://sdkman.io/jdks#amzn
  - sdk install java 25.0.4-amzn
  - sdk use java 25.0.4-amzn
